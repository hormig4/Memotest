  /*const disableCards = () => {
    setMatchedCards([...matchedCards, [firstCard, secondCard]]);
    resetCards();
  };*/


    /* useEffect(() => {
    if (matchedCards.length === cards.length / 2) {
      console.log("Ganaste!");
      setGameOver(()=>true);
     
      console.log("gameover: ", gameOver)
    }
  }, [matchedCards]);  //no funciona
  
  
  
  
  /*useEffect(() => {
    if (disabledCards.length === imagenes.length ) {
      console.log("Ganaste!");
    }
  }, [disabledCards]);*/




   /* useEffect(() => {
    if (matchedCards.length === imagenes.length ) {
      console.log("Ganaste!");
    }
  }, [matchedCards]);*/